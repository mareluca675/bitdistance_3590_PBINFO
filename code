#include <iostream>

using namespace std;

int main()
{
    unsigned long long n, i = 0;
    
    cin >> n;
    
    while(n % 2 == 0)
    {
        n = n >> 1;
    }
    
    while(n != 0)
    {
        i ++;
        n = n >> 1;
    }
    
    cout << i;

    return 0;
}
